overwrite: true
schema: "http://localhost:4000"
documents: './src/**/*.gql'
generates:
  src/generated/graphql-client.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node